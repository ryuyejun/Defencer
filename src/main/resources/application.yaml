#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: ${DB_URL}
#    username: ${DB_USERNAME}
#    password: ${DB_PW}
#  config:
#    import: optional:file:.env[.properties]
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true 잠시 이렇게 수정

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ryuyejun
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ryuyejun0301!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect